# Docs for the Azure Web Apps Deploy action: https://github.com/azure/functions-action
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Deploy Log Running VMs end-to-end

on:
  push:
    branches:
      - Dev
  workflow_dispatch:

env:
    AZURE_FUNCTIONAPP_PACKAGE_PATH: '.' # set this to the path to your web app project, defaults to the repository root
    resourceGroupName: RG-VMCountFADev
    laResourceGroup: DefaultResourceGroup-WEU
    laName: DefaultWorkspace-30483fd2-311e-4847-81bf-4fa79f8f8f44-WEU
    workspaceId: ab431c8494a14886aa0576f01ea23b5a

jobs:

  # Lint the Bicep file.
  lint:
    uses: ./.github/workflows/lint.yml

  # Deploy to the test environment.
  deploy-test:
    uses: ./.github/workflows/deploy.yml
    needs: lint
    with:
      environmentType: Test
      resourceGroupName: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_092C07438EA145F393206E3751F61908 }}
      laResourceGroup: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_092C07438EA145F393206E3751F61908 }}
      laName: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_092C07438EA145F393206E3751F61908 }}
      workspaceId: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_092C07438EA145F393206E3751F61908 }}