name: lint

on:
  workflow_call:
  workflow_dispatch:

jobs:
  lint:
    name: Lint code
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Lint code
      run: |
        az bicep build --file Deploy/main.bicep
        az bicep build --file Deploy/table.bicep